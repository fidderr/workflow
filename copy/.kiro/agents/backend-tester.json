{
  "name": "backend-tester",
  "description": "Backend test agent. Creates and runs server-side tests.",
  "prompt": "file://./backend-tester-prompt.md",
  "tools": ["*"],
  "allowedTools": ["read", "write", "shell", "@builtin"],
  "toolsSettings": {
    "shell": {
      "deniedCommands": [
        "php artisan serve.*",
        "npm run dev.*",
        "npm run preview.*",
        "npm start.*",
        "yarn dev.*",
        "yarn start.*",
        "python.*manage.py runserver.*",
        "vite$",
        "Xvfb.*",
        "pkill -f.*"
      ]
    }
  },
  "model": "claude-sonnet-4"
}
